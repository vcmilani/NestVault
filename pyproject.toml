[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["server", "tests"]
asyncio_mode = "auto"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "httpx>=0.27",
]
